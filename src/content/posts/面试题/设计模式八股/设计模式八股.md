---
title: 设计模式八股
published: 2025-12-08
description: ''
image: '../cover.png'
tags: ["八股"]
category: '八股'
draft: false 
lang: ''
---
# 什么是策略模式？
策略模式是一种行为型的设计模式，策略模式就是定义了一系列的算法，将算法封装起来，然后算法之间可以相互替换，调用者在不同的情况下使用不同的算法。降低了算法类和使用算法类之间的耦合。
## 代码结构：
````
// 1.策略接口
public interface PayStrategy {
    void pay();
}

// 2.具体策略
public class AliPay implements PayStrategy {
    public void pay() { 支付宝支付 }
}
public class WxPay implements PayStrategy {
    public void pay() { 微信支付 }
}

// 3.上下文
public class PayContext {
    private PayStrategy strategy;
    public PayContext(PayStrategy strategy) {
        this.strategy = strategy;
    }
    public void executePay() {
        strategy.pay();
    }
}

// 使用
new PayContext(new AliPay()).executePay();
new PayContext(new WxPay()).executePay();
````
